
$(document).ready(function () {
    $("#navbarSupportedContent ul").find("li").eq(6).addClass("active");

    var screenw = $(window).width();
    var screenh = $(window).height();
    // $(".bg-banner").css("height", (screenw / (1920 / 450)) + "px");
});